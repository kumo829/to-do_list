spring:
  data:
    mongodb:
      database: ${MONGO_DATABASE}
      host: ${MONGODB_USERS_HOST:172.20.155.178}
      port: ${MONGODB_USERS_PORT:27017}
      username: ${MONGODB_USERNAME:mongoadmin}
      password: ${MONGODB_PASSWORD:secret}

  hateoas:
    use-hal-as-default-json-media-type: false

